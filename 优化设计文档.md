# P2S 项目级 Vibe Coding 重构设计文档（2026-03-09 校准版）

## 0. 当前状态校准

这份文档基于当前仓库实现重写，目的是把早期规划与现状重新对齐，避免继续按已经过时的接口和心智模型推进。

### 已经成立的事实

- 当前系统已经是 **project / workspace file** 模型，而不是旧的单 doc 模型。
- agent / tool bridge 已经正式支持：
  - `list_projects`
  - `create_project`
  - `open_project`
  - `rename_project`
  - `get_project_state`
  - `read_workspace_file`
  - `create_workspace_file`
  - `save_workspace_file`
  - `rename_workspace_file`
  - `delete_workspace_file`
  - `propose_patch`
  - `search_block_ids`
- 当前工作区工具使用的是 **`path`**，不是旧文档里的 `workspace_id`。
- 当前工作区正文主格式已经是 **TOML workspace**，不是早期设计文档里的 JSON 脚本模型。
- 客户端已经具备这些 project 级辅助能力：
  - `update_plan`
  - `request_user_choice`
  - skills / subdocs
  - subagent / profile
  - 会话恢复与本地持久化
- `semantic` / `generated` / `manual` 已经进入 workspace type 枚举和持久化模型，但 **真正的 semantic 工具链尚未实现**。

### 目前仍未完成的部分

- 还没有正式的 `read_project_context` / `record_decision` / `project memory`。
- 还没有 `request_user_input` 这种多问题结构化输入；当前只有单次 choice popup。
- 还没有 `propose_design_options`。
- 还没有 `compile_semantic_file` / `propose_semantic_patch`。
- 还没有对 subagent 的 `workspace_scope` 强约束与结构化交付协议。

### 文档中的核心修正

本版文档明确做以下修正：

- 不再把 M1 视为“待开始”，而是视为 **已完成主体、仅剩收尾**。
- 不建议下一步直接做完整版 M4，而是先做 **M2.5 / M4-lite**。
- 所有新规划统一基于 **path + TOML + current project + client-side agent** 现状展开。

---

## 1. 目标与边界

当前阶段的总目标，不再是“把旧系统升级到 project/workspace 模型”，而是：

- 在已经落地的 project/workspace 基础上，补齐 **project 级上下文、决策、设计选择与语义中间层**。
- 保持现有 patch pipeline 为唯一执行层。
- 让 agent 从“能改文件”升级成“能稳定管理一个建筑项目”。

本阶段边界保持不变：

- **只管理本 mod 创建和维护的 project 内容**。
- **不做世界扫描 / 地形感知 / 邻居分析 / 准星采样**。
- **所有真实世界改动仍然必须走** `propose_patch -> preview -> apply/discard -> undo/redo`。
- **本阶段不做多人协作、project merge、远程同步**。
- **首版不做复杂 3D 设计预览**。

---

## 2. 当前架构分层

### 2.1 已有稳定底座

#### Project / Workspace

当前系统已经具备：

- project 列表与 project 持久化
- workspace file 列表与 path 命名
- workspace 类型枚举
- patch 预览 / 应用 / 丢弃 / undo / redo / checkpoint
- selected workspace 的本地 UI 状态

这意味着 M1 的主体已经完成，后续不应再推翻这层抽象。

#### Client Agent 能力

当前客户端 agent 已经具备：

- `get_project_state` 作为当前项目总览入口
- `read_workspace_file` 读取某个 path 对应的工作区正文
- `update_plan` 记录当前计划
- `request_user_choice` 请求用户选项
- subagent / profile / skill / subdoc 机制
- 会话恢复、plan 恢复、choice 恢复

这意味着 M2 与 M5 的“基础设施层”也已经不是空白。

### 2.2 当前最大的缺口

从产品能力角度看，真正缺的不是更多低层 patch 工具，而是三层中间能力：

1. **Project Context**：把 project 摘要、影响范围、约束、决策沉淀下来。
2. **Structured User Input**：当 agent 缺信息时，能以结构化问题收集用户输入，而不是只弹一个 choice。
3. **Design Intent Layer**：在进入 patch 前，先形成方案与语义草稿。

因此下一阶段不建议直接上 full M4，而应该先补：

- `M2.5：Project Context / Decision Memory`
- `M4.5-lite：Design Options`
- `M4-lite：Semantic Compile`

---

## 3. 模块现状与建议顺序

### M1：Project / Workspace API

**当前状态**：主体已完成。

已具备的核心能力：

- 当前 session 绑定当前 project
- 一个 project 下持有多个 workspace file
- 基于 path 的读写与 patch 定位
- project / workspace 基础 CRUD 与预览工作流

### M1 剩余收尾项

后续只保留小型收尾，不再把 M1 作为主线：

- 清理仍然遗留的旧命名或旧注释
- 收紧工具 schema 与错误返回的一致性
- 继续减少“UI 入口”和“tool 入口”重复分叉
- 让 workspace file 的 type / metadata 在 UI 中更稳定可见

---

### M2：Project Planning / Memory

**当前状态**：已完成基础设施，未完成 project memory 正式化。

#### 已有能力

- `update_plan`
- plan 在客户端状态与会话恢复中持久化
- `request_user_choice`
- subagent / skill / profile

#### 当前不足

- plan 仍然偏“当前回合可见”，还不是正式的 project memory。
- 还没有 `decisions / constraints / style_profile / important_refs` 这类长期上下文。
- 还没有统一的 `read_project_context`；agent 仍需先读 project，再自己总结。

### M2 的新目标

M2 不需要重做 planning UI，而是补齐 **project memory 层**：

#### 建议新增工具

1. `read_project_context`
   - 返回：
     - `project_summary`
     - `workspace_summaries`
     - `plan_summary`
     - `decision_summary`
     - `constraints_summary`
     - `open_questions`

2. `record_decision`
   - 将用户已经确认的方向写入 project 级 memory

3. `update_project_memory`
   - 用于维护：
     - `decisions`
     - `constraints`
     - `style_profile`
     - `terminology`
     - `important_refs`

#### 存储原则

- memory 应该归属于 project，而不是单个 session。
- 会话恢复只恢复当前运行态；project memory 是更长期的数据。
- plan 可以继续放在客户端会话里，但 decisions / constraints 应该落 project 层。

---

### M2.5：Design Context / Structured Input

**这是当前最推荐的下一步。**

灵感来自 Codex 的做法：不是只让模型弹一个二选一，而是可以请求一组结构化问题，让用户补齐缺失信息。

#### 目标

把当前的 `request_user_choice` 升级为更通用的输入层，用于：

- 设计风格确认
- 尺度 / 层高 / 材料倾向确认
- 缺失约束补全
- 方案分歧前的人机确认

#### 建议能力

新增 `request_user_input`，首版支持：

- 1~3 个问题
- 每题 2~3 个互斥选项
- 可选补充备注 / 自定义输入
- 返回结构化答案

#### 为什么先做它

因为它会直接提升：

- 设计型对话的成功率
- 后续 `propose_design_options` 的可用性
- future semantic IR 的输入质量

---

### M4.5：Design Preview / Choice

**当前状态**：基础 UI 已存在，缺正式工具与结果沉淀。

#### 现有可复用能力

- choice popup UI
- `request_user_choice`
- plan / subagent / workspace state 已可视化

#### 推荐先做 lite 版

先不要做 semantic patch，也不要做 3D preview。

先做正式工具：`propose_design_options`

输入建议：

- `goal`
- 可选 `workspace_scope`
- 可选 `constraints`

输出建议：

- `options`
  - `id`
  - `title`
  - `intent`
  - `affected_paths`
  - `estimated_change_size`
  - `risk`
  - `recommended`

#### 行为规则

- 方案生成后，继续复用现有 choice UI。
- 用户选定方案后，写入 project decisions。
- 首版只做文本方案，不做渲染图。

这一步本质上是：

- 让“设计分叉”先被看见
- 再决定要不要进入 patch 或 semantic compile

---

### M4：Semantic Building IR

**当前状态**：类型壳已在，工具链未开始。

本版不建议直接做完整版 M4，而是拆成两个阶段。

#### M4-lite（推荐先做）

目标：

- 引入最小语义层，但不一次性上完整实体图谱
- 保持与当前 path / TOML / generated file 体系兼容

##### 首版只要求这些事实成立

- workspace type 明确区分：`semantic` / `generated` / `manual`
- generated file 能记录 `generated_from`
- generated file 能记录 `manual_override`
- 系统能从 semantic file 产出 generated 草稿
- generated 草稿继续走现有 `propose_patch`

##### 首版建议只新增一个正式工具

1. `compile_semantic_file`
   - 输入：`path`
   - 输出：
     - `generated_path`
     - `draft_workspace_toml`
     - `summary`
     - `warnings`
     - `drift_status`

##### 暂缓的内容

- 暂缓 `propose_semantic_patch`
- 暂缓完整 `ProjectSemanticRoot / Area / Floor / Zone ...` 大图谱
- 暂缓复杂 compiler 命名协议
- 暂缓 compile-to-many

#### M4 phase 2（在 M4-lite 稳定之后）

再考虑：

- `propose_semantic_patch`
- area / floor / zone / facade / roof / component / constraint / anchor 的固定 schema
- semantic diff summary
- generated drift 管理

### M4-lite 的设计约束

#### 1）所有接口继续使用 `path`

不要回退到旧设计里的 `workspace_id`。当前系统已经稳定使用 path，这是更符合 IDE / 文件工作流的模型。

#### 2）继续基于 TOML workspace

semantic compile 的产物不应重新引入另一套与当前 workspace 流程割裂的底层格式。

#### 3）先把 `manual_override` 做成明确字段

当前 `generatedFrom` 已经存在，但 `manual_override` 还不应该只依赖隐式约定。

建议在 workspace metadata 或显式字段上正式记录它。

#### 4）编译是“生成草稿”，不是“直接覆盖”

即使是 semantic file，也只能：

- compile 出 generated 草稿
- 再进入现有 patch preview / apply 流程

---

### M5：Multi-Agent Architect

**当前状态**：subagent 基础设施已完成，项目级分工协议未完成。

#### 现有基础

- create / continue / get / delete subagent
- profile
- skill scope
- 并行安全工具分类

#### 下阶段建议

不要把 M5 做成“更多 subagent 按钮”，而要做成：

- 带 `workspace_scope` 或 `path_scope` 的受限任务
- 更稳定的结构化输出
- 主 agent 明确整合各 subagent 结果

#### 建议 profile

- `project-planner`
- `area-planner`
- `facade-designer`
- `circulation-designer`
- `component-designer`
- `patch-executor`

#### 建议输出协议

- `summary`
- `affected_paths`
- `recommendations`
- 可选 `design_option_draft`
- 可选 `semantic_compile_hint`
- 可选 `patch_draft`

---

## 4. 当前推荐实现顺序

建议按以下顺序推进，而不是继续按旧文档的 “M1 -> M2 -> M4” 线性理解：

1. **M1 收尾**
   - 只做命名、schema、UI 可见性收紧
2. **M2：Project Memory 正式化**
   - 增加 `read_project_context` / `record_decision` / `update_project_memory`
3. **M2.5：Structured User Input**
   - 用 `request_user_input` 替代只支持单次 choice 的交互
4. **M4.5-lite：Design Options**
   - 增加 `propose_design_options`
5. **M4-lite：Semantic Compile**
   - 先做 `compile_semantic_file`
6. **M5：Scoped Subagent Delivery**
   - 加 `path_scope` 与结构化输出协议
7. **M4 phase 2：Semantic Patch**
   - 最后再考虑 full semantic editing

---

## 5. 验收标准（更新版）

### 近阶段验收标准

当以下能力成立时，可以认为下一阶段完成：

- agent 能一键读取 project context，而不是只读 project state 原始字段
- 用户确认过的设计方向能沉淀为 decisions / constraints
- agent 可以发起结构化用户输入，而不只是简单 choice
- 大改动前可以先返回 2~3 个设计方案
- 用户选定方案后，结果能写回 project memory
- semantic file 至少可以 compile 成 generated 草稿
- generated file 的 `generated_from` / `manual_override` 状态在 UI 中可见

### 明确不作为本阶段验收项

以下内容不应阻塞当前路线：

- 完整语义实体全量覆盖
- 3D 设计预览
- 多人协作
- 自动世界感知
- 无限层级 subagent 编排

---

## 6. 与当前实现保持一致的规则

为防止文档再次过时，后续所有设计都应遵守以下规则：

- 新工具优先复用当前 project/workspace/path 模型
- 除非有充分理由，不引入新的“ID-only” 文件定位体系
- 除非必须，不绕开当前 patch pipeline
- plan / choice / subagent 优先在现有客户端能力上演进
- 新文档若描述工具输入输出，必须与当前源码和 schema 对齐后再提交

---

## 7. 总结

当前项目已经跨过“从单 doc 升级到 project/workspace”的阶段，真正值得投入的下一步不是 full semantic IR，而是：

- 先补 **project memory**
- 再补 **structured user input**
- 再补 **design options**
- 最后以 **M4-lite semantic compile** 的方式进入语义层

这样推进的收益是：

- 不会推翻当前已经稳定的 project/workspace/path/TOML 体系
- 能直接复用已有的 plan / choice / skill / subagent 基础设施
- 能更快提升 agent 在真实建筑项目里的稳定性与可控性
- 让 M4 从“理想中的大重构”变成“可增量落地的下一阶段能力”
