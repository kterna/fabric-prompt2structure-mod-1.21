# P2S 项目级 Vibe Coding 重构设计文档

## 1. 目标与边界

本次重构的目标，是把当前以 `session + 单工作区/坐标范围 + patch` 为核心的交互方式，升级为：

- **一个 session 对应一个当前 project**
- **一个 project 下包含多个 workspace file**，例如 A 区域、B 区域、楼层、立面、组件
- **project 下所有 workspace file 都是平级资源，不存在 active workspace file**
- **agent 按 project 级上下文工作，可以随时读取和编辑任意一个 workspace file**

本次设计的边界也一并明确：

- **只管理本 mod 创建和维护的 project 内容**
- **不支持读取原生世界方块，不做世界感知，不做准星/地形/邻居扫描**
- **现有 patch pipeline 仍然是唯一执行层**，也就是世界中的真实建造和修改仍然通过现有 `propose_patch -> preview -> apply/discard -> undo/redo` 流程完成
- **本次不做多人协作和 project 合并**

---

## 2. 模块与分支拆分

本轮建议拆成 5 个模块，对应 5 个功能分支：

### M1：Project / Workspace API

**分支名**：`feat/project-workspace-api`

职责：

- 把当前“一个工作区就是一个坐标范围”的模型改成“一个 project 下有多个 workspace file”
- 把目前分散在 UI 按钮、session action、agent tool bridge 三处的入口统一到一套 agent 可调用的正式工具上
- 保留现有 patch 执行层，但把 patch 的作用目标从“当前 doc”明确改成“当前 project 下的某个 workspace file”

### M2：Project Planning / Memory

**分支名**：`feat/project-planning-memory`

职责：

- 把当前依赖聊天历史的会话逻辑升级为 **project 级计划与记忆**
- 让 agent 能先读取 project 摘要，再有步骤地管理多个 workspace file

### M4：Semantic Building IR

**分支名**：`feat/semantic-ir`

职责：

- 引入语义层建筑模型
- 让用户的对话目标先落到语义层，再编译成 generated workspace file，最后走现有 patch 流程

### M4.5：Design Preview / Choice

**分支名**：`feat/design-preview-choices`

职责：

- 在大改动前先生成多个设计方案，供用户选择后再落 patch

### M5：Multi-Agent Architect

**分支名**：`feat/multi-agent-architect`

职责：

- 把现有 subagent 机制升级为“项目级建筑工程队”

### 依赖顺序

- `M1 -> M2 -> M4`
- `M4.5` 轻量版依赖 `M2`，完整版依赖 `M4`
- `M5` 依赖 `M1 + M2`，推荐同时接入 `M4`

---

## 3. M1：Project / Workspace API

### 3.1 设计目标

M1 是整轮重构的基础模块。

它做两件事：

1. **模型重构**：从 `session -> 当前坐标范围 -> 当前 doc` 改成 `session -> 当前 project -> 多个平级 workspace file`
2. **入口统一**：把原来只能通过按钮/session action 做的文档操作，整理成 agent 可直接调用的正式工具

这里不再单独拆一个“M0 基础契约收敛”。这部分工作直接并入 M1 完成。

### 3.2 核心心智模型

重构前：

- 一个 session 本质上只有一个主工作区
- 多 doc 更像附属能力
- agent 真正能桥接的工具很少，很多操作还是 UI 行为

重构后：

- 一个 session 绑定一个 **当前 project**
- 一个 project 下有多个 **workspace file**
- 所有 workspace file 在 project 下平级存在
- agent 对任意 workspace file 的读写都通过显式 `workspace_id` 指定目标
- 一次 patch 仍然只针对一个 workspace file

### 3.3 数据模型

#### `ProjectState`

表示当前 session 打开的项目。

字段固定为：

- `id`
- `name`
- `description`
- `created_at`
- `updated_at`
- `default_origin`：可选，仅用于 build/apply 对齐
- `tags`
- `workspace_files`
- `metadata`

#### `WorkspaceFileState`

表示 project 下的一个文件。

字段固定为：

- `id`
- `name`
- `path`
- `type`
- `area_tag`
- `origin`：可选
- `size`：可选
- `current`
- `revision`
- `pending_patch`
- `undo_stack`
- `redo_stack`
- `summary`
- `depends_on`
- `generated_from`
- `metadata`

#### `type` 取值

首版固定以下类型，不额外扩展：

- `layout`
- `floor`
- `facade`
- `component`
- `semantic`
- `generated`
- `manual`
- `shared`

### 3.4 存储组织

M1 先不引入复杂 project 列表管理。首版只要求：

- 当前 session 对应一个当前 project
- project 的内部文件采用统一命名规则

推荐目录约定：

- `workspace/areas/a-zone.json`
- `workspace/areas/b-zone.json`
- `workspace/floors/f1.json`
- `workspace/floors/f2.json`
- `workspace/facades/north.json`
- `workspace/components/stair-a.json`

说明：

- 这些 `path` 是 project 内部的逻辑路径，不要求立即映射为真实磁盘目录结构
- 对当前代码实现来说，可以先继续存在 `SessionManager` 的内存结构里，只是对外统一成 project/workspace file 语义

### 3.5 Agent 工具集

M1 刻意只保留**最小工具集**，不引入过多“平台型工具”。

#### 1）`get_project_state`

用途：

- 获取当前 project 的总览
- 让 agent 知道当前有哪些 workspace file、哪些文件有 pending patch，以及每个文件的基本摘要

输入：

- 无；默认读取当前 session 打开的 project

输出：

- `project`
- `workspace_files`
- `pending_workspace_ids`
- `summary`

#### 2）`read_workspace_file`

用途：

- 读取某个 workspace file 的内容

输入：

- `workspace_id`
- 可选 `committed`，默认 `true`

输出：

- `workspace_id`
- `name`
- `type`
- `path`
- `revision`
- `state`

说明：

- 这是 `read_workspace_state` 的正式替代
- 旧 `read_workspace_state` 先保留为兼容别名，内部转发到 `read_workspace_file`

#### 3）`create_workspace_file`

用途：

- 在当前 project 下创建新的 workspace file

输入：

- `name`
- `path`
- `type`
- 可选 `copy_from_workspace_id`

输出：

- `workspace_id`
- `created`

#### 4）`rename_workspace_file`

用途：

- 重命名 workspace file

输入：

- `workspace_id`
- `name`
- 可选 `path`

输出：

- `workspace_id`
- `name`
- `path`

#### 5）`delete_workspace_file`

用途：

- 删除 workspace file

输入：

- `workspace_id`

输出：

- `workspace_id`
- `deleted`

删除规则固定为：

- 不允许删除存在 pending patch 的 workspace file
- 不允许删除唯一剩余的 workspace file

#### 6）`propose_patch`

用途：

- 对指定 workspace file 提交 patch 提案

输入：

- `workspace_id`
- `patch`

说明：

- `workspace_id` 为必填；agent 必须显式指定要修改的目标 workspace file
- patch 结构保持现有格式，不在 M1 修改

#### 7）`search_block_ids`

用途：

- 当 agent 不确定 palette/block id 时查询候选方块

输入输出保持现有设计

### 3.6 为什么不加这些工具

M1 **不加入** 以下工具：

- `list_projects`
- `create_project`
- `open_project`
- `switch_workspace_file`
- `duplicate_workspace_file`
- `set_workspace_metadata`

原因：

- `switch_workspace_file` 背后的前提是存在 active workspace file，但本设计明确取消这个概念
- 其他工具会把首版做成“项目管理平台”，而不是“可用的项目级 agent 工作流”
- 当前阶段只需要先把一个 session 对应一个当前 project 跑通
- 后续若有需要，再在 M1 的后续迭代补充

### 3.7 Session / UI / 网络同步调整

M1 需要同步改三层：

#### SessionManager

- session 内部主状态改为 `currentProject`
- 原 `docs` 逻辑迁移为 `workspaceFiles`
- 不再维护 `activeWorkspaceId`；所有 workspace file 在 project 下平级存在
- 原 `read_workspace_state`、`doc_create`、`doc_switch`、`doc_rename` 的行为统一改到 project/workspace file 模型下

#### ClientAgentManager

- 工具桥接改为正式支持：
  - `get_project_state`
  - `read_workspace_file`
  - `create_workspace_file`
  - `rename_workspace_file`
  - `delete_workspace_file`
  - `propose_patch`
  - `search_block_ids`

#### Chat/UI

- 左侧文件列表展示 workspace file 列表，而不是旧 doc 列表
- `+ New File` 行为改为创建 workspace file
- 文件列表中的点击或选中，只影响本地查看上下文，不构成服务端的 active workspace 状态
- 每个文件的 pending patch 状态、revision、类型仍然在 UI 中可见

### 3.8 兼容策略

为避免一次性推翻旧逻辑，M1 采用以下兼容策略：

- 旧 session 启动时自动映射为一个当前 project
- 旧的主结构脚本默认映射为一个 workspace file：`main`
- 旧 `read_workspace_state` 仍可调用，但返回结果中补齐 `workspace_id`
- 旧 UI 中的 doc 字段可以保留一轮兼容，但新字段统一使用 workspace 命名

### 3.9 M1 验收标准

- agent 能获取当前 project 总览
- agent 能读取 A 区域 / B 区域等不同 workspace file
- agent 能创建、读取、重命名、删除 workspace file，并对任意指定文件发起 patch
- `propose_patch` 能明确作用于指定 workspace file
- 旧 session 可以被自动迁移到新的 project/workspace 模型下继续使用

---

## 4. M2：Project Planning / Memory

### 4.1 设计目标

M2 要解决的问题是：

- 当前 agent 主要依赖聊天历史
- 当项目变大、workspace file 变多时，单靠历史消息无法稳定管理整个 project
- agent 需要有 project 级计划和记忆，而不是只知道“当前文件是什么”

因此，M2 的核心原则固定为：

- **plan 是 project 级，不是 workspace 级**
- **memory 是 project 级，不是临时聊天级**
- **agent 遇到中/大任务时，先读 project context，再决定读哪些 workspace file**

### 4.2 数据模型

#### `ProjectPlanState`

字段固定为：

- `project_id`
- `goal`
- `success_criteria`
- `status`
- `affected_workspace_ids`
- `steps`
- `risks`
- `open_questions`
- `last_updated_at`

#### `PlanStep`

字段固定为：

- `id`
- `title`
- `status`：`pending | in_progress | done | blocked`
- `workspace_scope`
- `expected_output`
- `notes`

#### `ProjectMemoryState`

字段固定为：

- `project_id`
- `decisions`
- `constraints`
- `style_profile`
- `terminology`
- `open_questions`
- `important_refs`
- `last_updated_at`

### 4.3 工具集

M2 也保持工具数量克制，只加入 project 级 plan / memory 必需工具。

#### 1）`read_project_context`

用途：

- 读取项目级上下文，而不是逐个文件盲读

输入：

- 可选 `workspace_scope`
- 可选 `include_plan`，默认 `true`
- 可选 `include_memory`，默认 `true`

输出：

- `project_summary`
- `workspace_summaries`
- `plan_summary`
- `memory_summary`

#### 2）`explain_plan`

用途：

- 为当前 project 的某个目标生成结构化计划

输入：

- `goal`
- 可选 `workspace_scope`
- 可选 `mode`：`quick | full`

输出：

- `plan`
- `summary`
- `affected_workspace_ids`
- `needs_user_choice`

说明：

- 这里不再是“accepted=true”的空壳工具，而是要真正写出结构化 plan

#### 3）`get_plan`

用途：

- 读取当前 project 的 plan

#### 4）`update_plan_step`

用途：

- 更新某个计划步骤的状态与备注

输入：

- `step_id`
- `status`
- 可选 `notes`

#### 5）`get_memory`

用途：

- 读取当前 project 的 memory

#### 6）`update_memory`

用途：

- 写入或更新约束、术语、风格摘要等长期上下文

输入：

- `decisions`
- `constraints`
- `style_profile`
- `terminology`
- `important_refs`

#### 7）`record_decision`

用途：

- 把用户已经确认的关键决策落到 project memory 中

输入：

- `title`
- `value`
- `source`

### 4.4 Agent 行为规则

规则固定如下：

- **小任务**：只涉及单个明确 workspace file 的局部改动，可以直接 `read_workspace_file + propose_patch`
- **中任务**：涉及多个 file，或用户表述明显是项目级需求，必须先 `read_project_context`
- **大任务**：涉及整体风格、整体布局、多个区域联动，必须先 `explain_plan`

具体判断标准：

- 出现“整个项目”“整体风格”“A/B 区域联动”“多个楼层”时，一律按中/大任务处理
- 一次改动影响 2 个及以上 workspace file 时，plan 中必须写出 `affected_workspace_ids`

### 4.5 存储策略

M2 不把 plan / memory 存进普通 workspace file，而是放到 project 系统元数据里：

- `workspace/.project/plan.json`
- `workspace/.project/memory.json`

这样做的目的：

- 避免把“系统级会话信息”混入建筑结构文件
- 让 workspace file 仍然只关心结构内容

### 4.6 M2 验收标准

- agent 可以先读 project context，再决定读哪些 workspace file
- `explain_plan` 返回结构化 project 级计划
- 用户确认后的关键选择可以被写入 memory
- 长任务中即使聊天历史被裁剪，project 级 plan 和 memory 仍然完整保留

---

## 5. M4：Semantic Building IR（详细版）

### 5.1 设计目标

M4 的目标不是替换 patch engine，而是引入一层**语义建筑中间表示**，让用户和 agent 操作的是“建筑意图”，不是长期直接操作 `box / plane / line / points`。

M4 完成后，系统会形成三层结构：

1. **语义层**：表达项目、区域、楼层、功能区、立面、屋顶、组件、约束
2. **生成层**：把语义层编译成 generated workspace file
3. **执行层**：对 generated workspace file 继续走现有 patch pipeline

### 5.2 为什么要加语义层

当前低层 VBS 很适合执行，但不适合作为长期项目协作主表示。问题主要有三类：

#### 1）用户需求是语义性的

用户更可能说：

- “把 A 区域改成入口大厅”
- “让 B 区域和 A 区域之间增加连桥”
- “北立面做更现代的开窗节奏”

而不是说：

- “给某个 part 插入 3 条 plane action”

#### 2）多轮 patch 后会越来越难维护

- generated VBS 会随着 patch 越来越长
- planner 不容易稳定复用旧结构
- 人类也难以快速审阅“这个项目到底长什么样”

#### 3）没有稳定的高层审阅面

目前 preview 更像工程 patch 预览，不是设计审阅预览。

### 5.3 M4 的明确边界

M4 **做的事**：

- 新增语义层文件格式
- 支持语义 patch
- 支持从语义文件编译生成 generated workspace file
- 让 generated file 继续复用现有 patch / preview / apply 机制

M4 **不做的事**：

- 不直接操作世界
- 不替换现有 `PatchModels.StructurePatch`
- 不要求所有项目都强制迁移到语义层
- 不在首版做复杂 3D 预览

### 5.4 文件类型与关系

M4 固定三类 workspace file：

#### `semantic`

用于保存建筑语义。

特点：

- 供 agent 规划和对话使用
- 供人类审阅项目结构使用
- 不直接用于世界建造

#### `generated`

由 `semantic` 文件编译而来。

特点：

- 内容仍然是现有 VBS/结构脚本
- 是 patch engine 的主要输入
- 可以被 preview / apply / undo / redo

#### `manual`

表示人工直接维护的底层结构文件。

特点：

- 不参与语义编译
- 适合少量特殊部件或过渡期兼容

### 5.5 语义文件映射规则

为了降低首版复杂度，M4 固定采用：

- **一个 area 语义文件，对应一个 area 生成文件**

不做一对多编译。

具体约定：

- `workspace/semantic/project.json`：项目总语义文件，仅保存全局信息和 area 索引
- `workspace/semantic/areas/a-zone.json`：A 区域语义文件
- `workspace/generated/areas/a-zone.generated.json`：A 区域生成文件

规则固定为：

- `project.json` 不直接编译为 generated file
- 每个 `workspace/semantic/areas/*.json` 必须只对应一个 `workspace/generated/areas/*.generated.json`
- generated file 必须记录 `generated_from = <semantic_workspace_id>`

### 5.6 语义实体范围

首版语义 IR 固定只支持以下实体，不扩展：

- `ProjectSemanticRoot`
- `Area`
- `Floor`
- `Zone`
- `Circulation`
- `Facade`
- `Roof`
- `Component`
- `Constraint`
- `Anchor`

### 5.7 语义实体定义

#### `ProjectSemanticRoot`

字段固定为：

- `project_id`
- `name`
- `style_profile`
- `default_palette_profile`
- `area_ids`
- `shared_component_ids`
- `global_constraints`

用途：

- 定义整个项目的默认风格、默认材料语义、区域索引和全局约束

#### `Area`

字段固定为：

- `id`
- `name`
- `role`
- `base_origin`
- `footprint_size`
- `default_floor_height`
- `style_profile`
- `floor_ids`
- `facade_ids`
- `roof_id`
- `component_ids`
- `constraints`

说明：

- `Area` 是编译单位
- 一个 area 对应一个 semantic area file 和一个 generated area file

#### `Floor`

字段固定为：

- `id`
- `area_id`
- `level_index`
- `y_base_mode`：`auto | absolute`
- `y_base`
- `height`
- `zone_ids`
- `circulation_ids`

说明：

- `height` 是编译成楼层几何的直接依据
- `level_index` 用于自动堆叠时排序

#### `Zone`

字段固定为：

- `id`
- `floor_id`
- `name`
- `purpose`
- `bbox`
- `ceiling_height`
- `wall_profile`
- `opening_policy`
- `component_refs`
- `constraints`

说明：

- `bbox` 使用局部坐标，基于所属 `Area` 的局部平面定义范围
- `opening_policy` 固定值：`none | sparse | normal | open`

#### `Circulation`

字段固定为：

- `id`
- `floor_id`
- `type`：`corridor | stairs | bridge | entrance_hall`
- `from_anchor`
- `to_anchor`
- `width`
- `slope_mode`：`flat | stairs`
- `constraints`

#### `Facade`

字段固定为：

- `id`
- `area_id`
- `orientation`：`north | south | east | west`
- `style`
- `window_pattern`：`solid | grid | strip | arched`
- `material_profile`
- `rhythm`
- `constraints`

#### `Roof`

字段固定为：

- `id`
- `area_id`
- `type`：`flat | gable | hip | shed`
- `height`
- `material_profile`
- `constraints`

#### `Component`

字段固定为：

- `id`
- `area_id`
- `type`：`stair | column_grid | entrance_portico | bridge | tower | window_module`
- `anchor_id`
- `parameters`
- `constraints`

#### `Constraint`

字段固定为：

- `id`
- `target_ref`
- `kind`：`size_limit | keep_alignment | keep_axis | protect_part | symmetry | material_lock`
- `value`
- `hard`

#### `Anchor`

字段固定为：

- `id`
- `scope`：`area | floor | zone | generated_part`
- `ref`
- `local_pos`

用途：

- 让 circulation、component、跨实体连接拥有稳定引用点

### 5.8 语义工具集

M4 只新增两个正式工具，避免工具过多：

#### 1）`propose_semantic_patch`

用途：

- 修改语义文件，而不是直接修改 generated file

输入：

- `workspace_id`
- `intent`
- `operations`
- 可选 `message_to_user`

首版 `operations` 固定只支持：

- `add_entity`
- `update_entity`
- `remove_entity`
- `move_entity`
- `add_constraint`
- `update_constraint`
- `remove_constraint`
- `link_ref`
- `unlink_ref`

输出：

- `ok`
- `semantic_preview`
- `affected_entities`
- `warnings`

#### 2）`compile_semantic_file`

用途：

- 把某个 semantic area file 编译为对应的 generated area file 草稿

输入：

- `workspace_id`

输出：

- `generated_workspace_id`
- `draft`
- `summary`
- `warnings`
- `drift_status`

说明：

- 这里只输出 generated 草稿，不直接 apply 世界
- 生成后的底层修改仍然要进入现有 `propose_patch` 流程

### 5.9 编译器行为

编译器的职责固定为：

1. 读取 `ProjectSemanticRoot`
2. 读取目标 `Area` 语义文件
3. 校验引用、约束、楼层顺序、bbox 合法性
4. 生成标准化中间布局
5. 产出稳定的 generated VBS 结构

### 5.10 编译命名规则

generated file 中的 part 命名必须稳定，规则固定为：

- `area.<area_id>.shell`
- `area.<area_id>.floor.<floor_id>.slab`
- `area.<area_id>.zone.<zone_id>.walls`
- `area.<area_id>.zone.<zone_id>.openings`
- `area.<area_id>.circulation.<circulation_id>`
- `area.<area_id>.facade.<orientation>`
- `area.<area_id>.roof.<roof_id>`
- `area.<area_id>.component.<component_id>`

首版实现不得随意变更命名模式，否则 patch diff 会不稳定。

### 5.11 语义到 generated 的映射规则

#### `Area`

负责：

- 定义整体边界
- 生成外壳和楼层容器

#### `Floor`

负责：

- 定义每层的基准高度和层高
- 生成 slab 和楼层范围

#### `Zone`

负责：

- 定义区域内部空间块
- 生成区块墙体、可选开口预留、功能分区 part

#### `Circulation`

负责：

- 生成走廊、楼梯、桥接、入口厅等连接结构

#### `Facade`

负责：

- 生成外立面开窗节奏和外立面材料布置

#### `Roof`

负责：

- 生成屋顶形态

#### `Component`

负责：

- 生成可复用局部部件，例如楼梯、门廊、塔楼、窗模块

### 5.12 手工修改 generated file 的规则

generated file 的默认权威来源是 semantic file。

因此规则固定为：

- 当用户直接对 generated file 做 patch 时，该 generated file 标记 `manual_override = true`
- 只要 `manual_override = true`，后续 `compile_semantic_file` 就**不能直接覆盖**该 generated file
- 此时 `compile_semantic_file` 只能返回新草稿和 warning，提醒用户该文件已发生手工漂移

### 5.13 漂移规则

M4 不单独增加“漂移检查工具”，而是把漂移信息并入 `compile_semantic_file` 的返回值。

`drift_status` 固定为：

- `clean`
- `manual_override`
- `missing_generated`
- `compile_conflict`

### 5.14 运行流程

#### 场景 A：用户做高层设计修改

1. agent 读取 `read_project_context`
2. agent 读取相关 semantic file
3. agent 调 `propose_semantic_patch`
4. agent 调 `compile_semantic_file`
5. agent 对对应 generated file 调 `propose_patch`
6. 用户 preview / apply

#### 场景 B：用户直接做底层结构修补

1. agent 直接 `read_workspace_file`
2. agent `propose_patch`
3. 如果该 file 是 generated file，则标记 `manual_override = true`

#### 场景 C：语义文件更新后重新编译

1. agent `compile_semantic_file`
2. 若 `drift_status = clean`，可正常进入 generated patch 提案
3. 若 `drift_status = manual_override`，必须先提示用户，不得静默覆盖

### 5.15 UI / 审阅要求

M4 首版不做可视化 3D 预览，但必须满足以下审阅要求：

- semantic patch 有文字摘要
- compile 结果有文字摘要
- generated file 有 `generated_from` 和 `manual_override` 状态可见
- 用户能分辨“当前看的这个文件是 semantic / generated / manual”

### 5.16 M4 验收标准

- 能创建 semantic workspace file 并读取
- 能对 semantic file 做 `propose_semantic_patch`
- 能将 semantic area file 编译成对应 generated area file 草稿
- generated file 可继续走现有 patch pipeline
- 直接修改 generated file 后会触发 `manual_override`
- 再次编译时不会静默覆盖手工修改

---

## 6. M4.5：Design Preview / Choice

### 6.1 目标

在大改动前先给出多个设计方案，而不是只给一个 patch 结果。

### 6.2 输入输出

新增工具：`propose_design_options`

输入：

- `goal`
- 可选 `workspace_scope`

输出：

- `options`

每个 option 固定字段：

- `id`
- `title`
- `intent`
- `affected_workspace_ids`
- `estimated_change_size`
- `risk`
- `recommended`

### 6.3 行为规则

- 用户选项确认继续复用现有 `request_user_choice`
- 用户选定后，结果写入 project memory 的 `decisions`
- 首版只做文本方案预览，不做 3D 渲染

---

## 7. M5：Multi-Agent Architect

### 7.1 目标

把现有 subagent 升级成按 project / workspace scope 工作的子 agent。

### 7.2 固定 profile

首版固定以下 profile：

- `project-planner`
- `area-planner`
- `facade-designer`
- `circulation-designer`
- `component-designer`
- `patch-executor`

### 7.3 规则

- 只有主 agent 能创建 subagent
- subagent 不允许再创建 subagent
- subagent 默认只读自己被分配的 `workspace_scope`
- subagent 输出必须结构化，字段固定为：
  - `summary`
  - `affected_workspace_ids`
  - `recommendations`
  - 可选 `semantic_patch_draft`
  - 可选 `patch_draft`

---

## 8. 实现顺序建议

建议按以下顺序推进：

1. **先做 M1**
   - 这是后续所有 project 级能力的底座
2. **再做 M2**
   - 让 agent 真正能 project 级工作
3. **然后做 M4**
   - 引入语义层
4. **之后做 M4.5**
   - 让大改动具备方案选择能力
5. **最后做 M5**
   - 把 subagent 升级为工程队

---

## 9. 总结

这次重构的核心，不是增加更多工具，而是把系统的工作对象从“一个坐标工作区”改成“一个 project 下的一组平级 workspace file”。

整个架构会变成：

- **M1**：先把 project / workspace file 跑通
- **M2**：再让 agent 具备 project 级计划与记忆
- **M4**：再把建筑意图提升到语义层
- **M4.5 / M5**：最后补方案协作和多 agent

这样做的结果是：

- 用户可以通过对话管理整个建筑项目，而不是只改一个当前结构脚本
- agent 可以像管理代码库那样管理多个区域文件
- 所有 workspace file 在 project 下平级存在，agent 不依赖 active file 状态
- 语义层可以承接更高层的建筑意图
- 现有 patch 执行层仍然保留，不需要推翻重写